# Workspace Instructions

- Build extension with `npm run compile`.
- Main entry: `src/extension.ts`.
- PostgreSQL service code lives in `src/postgresService.ts`.
- Tree view provider lives in `src/explorer.ts`.
